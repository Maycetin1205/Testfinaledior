// What the editor needs of the capture: its columns and the columns the
// lookup window of one column shows.
export { coerceCaptureColumns, type CaptureColumn } from './column'
export { windowColumnsIn, type CaptureContext } from './row'
